<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Edit | <%= movie.title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">

    <style>
        :root {
            --bg-color: #0a0a0a;
            --surface-color: #1a1a1a;
            --primary-color: #e50914;
            --text-main: #ffffff;
            --text-dim: #a0a0a0;
            --border-color: #333333;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-main);
            font-family: 'Inter', sans-serif;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        /* Header styled as a breadcrumb bar */
        .edit-header {
            background: var(--surface-color);
            border-bottom: 1px solid var(--border-color);
            padding: 1rem 2rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .back-link {
            color: var(--text-dim);
            text-decoration: none;
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            transition: color 0.2s;
        }

        .back-link:hover {
            color: var(--text-main);
        }

        /* Layout: Side-by-side preview and form */
        .main-content {
            flex: 1;
            padding: 3rem 1rem;
        }

        .form-container {
            max-width: 900px;
            margin: auto;
            background: var(--surface-color);
            border: 1px solid var(--border-color);
            border-radius: 16px;
            overflow: hidden;
            display: flex;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
        }

        .preview-pane {
            width: 35%;
            background: #222;
            padding: 2rem;
            border-right: 1px solid var(--border-color);
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
        }

        .preview-pane img {
            width: 100%;
            border-radius: 8px;
            margin-bottom: 1.5rem;
            box-shadow: 0 10px 20px rgba(0,0,0,0.3);
        }

        .edit-pane {
            flex: 1;
            padding: 2.5rem;
        }

        .form-control {
            background: #222 !important;
            border: 1px solid var(--border-color) !important;
            color: white !important;
            padding: 0.75rem;
            border-radius: 8px;
            font-size: 0.9rem;
        }

        .form-label {
            color: var(--text-dim);
            font-size: 0.8rem;
            font-weight: 600;
            margin-bottom: 0.4rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .btn-update {
            background: var(--primary-color);
            color: white;
            border: none;
            border-radius: 8px;
            padding: 0.8rem;
            font-weight: 700;
            width: 100%;
            margin-top: 1rem;
            transition: transform 0.2s;
        }

        .btn-update:hover {
            filter: brightness(1.1);
            transform: translateY(-2px);
        }

        @media (max-width: 768px) {
            .form-container { flex-direction: column; }
            .preview-pane { width: 100%; border-right: none; border-bottom: 1px solid var(--border-color); }
        }
    </style>
</head>

<body>

    <div class="edit-header">
        <a href="/" class="back-link">
            <i class="bi bi-arrow-left"></i> Back to Catalog
        </a>
        <span class="fw-bold">Editor Mode</span>
        <div style="width: 100px;"></div> <!-- Spacer for centering -->
    </div>

    <div class="main-content">
        <div class="form-container">
            <!-- Visual preview of the current movie -->
            <div class="preview-pane">
                <img src="<%= movie.poster %>" alt="Current Poster">
                <h5 class="fw-bold m-0"><%= movie.title %></h5>
                <p class="text-dim small mt-2"><%= movie.genre %> • <%= movie.releaseYear %></p>
            </div>

            <div class="edit-pane">
                <h4 class="fw-bold mb-4">Edit Metadata</h4>
                <form action="/update/<%= movie.id %>" method="POST" enctype="multipart/form-data">
                    <div class="row g-3">
                        <div class="col-12">
                            <label class="form-label">Title</label>
                            <input class="form-control" name="title" value="<%= movie.title %>" required>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Genre</label>
                            <input class="form-control" name="genre" value="<%= movie.genre %>">
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Rating</label>
                            <input class="form-control" name="rating" value="<%= movie.rating %>">
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Director</label>
                            <input class="form-control" name="director" value="<%= movie.director %>">
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Release Year</label>
                            <input class="form-control" name="releaseYear" value="<%= movie.releaseYear %>">
                        </div>
                        <div class="col-12">
                            <label class="form-label">Description</label>
                            <textarea class="form-control" name="description" rows="4"><%= movie.description %></textarea>
                        </div>
                        <div class="col-12">
                            <label class="form-label">Update Poster</label>
                            <input class="form-control" type="file" name="poster">
                            <div class="form-text text-muted" style="font-size: 0.7rem;">Leave empty to keep current poster</div>
                        </div>
                        <div class="col-12 pt-3">
                            <button class="btn btn-update shadow-sm">Save Changes</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>

</body>
</html>
