<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <title>CineStream | Dashboard</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">

    <style>
        :root {
            --bg-color: #0a0a0a;
            --surface-color: #1a1a1a;
            --primary-color: #e50914;
            --text-main: #ffffff;
            --text-dim: #a0a0a0;
            --border-color: #333333;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-main);
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            margin: 0;
            padding-bottom: 50px;
        }

        .navbar {
            background: rgba(10, 10, 10, 0.8) !important;
            backdrop-filter: blur(12px);
            border-bottom: 1px solid var(--border-color);
            padding: 1rem 2rem;
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .navbar-brand {
            font-weight: 800;
            color: var(--primary-color) !important;
            letter-spacing: -1px;
            font-size: 1.5rem;
        }

        .search-container {
            position: relative;
            max-width: 400px;
            width: 100%;
        }

        .search-input {
            background: #222 !important;
            border: 1px solid var(--border-color) !important;
            color: white !important;
            border-radius: 8px;
            padding-left: 2.5rem;
            font-size: 0.9rem;
            transition: all 0.2s;
        }

        .search-input:focus {
            border-color: var(--primary-color) !important;
            box-shadow: 0 0 0 2px rgba(229, 9, 20, 0.2);
        }

        .search-icon {
            position: absolute;
            left: 0.8rem;
            top: 50%;
            transform: translateY(-50%);
            color: var(--text-dim);
        }

        .form-card {
            background: var(--surface-color);
            border: 1px solid var(--border-color);
            border-radius: 12px;
            padding: 2rem;
            margin: 3rem auto;
            max-width: 800px;
        }

        .form-control, .form-select {
            background: #222 !important;
            border: 1px solid var(--border-color) !important;
            color: white !important;
            padding: 0.8rem;
            border-radius: 8px;
        }

        .form-control::placeholder {
            color: #666;
        }

        .btn-primary {
            background: var(--primary-color);
            border: none;
            font-weight: 600;
            padding: 0.8rem;
            border-radius: 8px;
            transition: filter 0.2s;
        }

        .btn-primary:hover {
            filter: brightness(1.2);
            background: var(--primary-color);
        }

        .section-title {
            font-size: 1.25rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            color: var(--text-main);
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .section-title::before {
            content: '';
            width: 4px;
            height: 24px;
            background: var(--primary-color);
            border-radius: 2px;
        }

        .movie-card {
            background: var(--surface-color);
            border: 1px solid var(--border-color);
            border-radius: 12px;
            overflow: hidden;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            height: 100%;
            position: relative;
        }

        .movie-card:hover {
            transform: translateY(-10px) scale(1.02);
            border-color: #444;
            box-shadow: 0 20px 40px rgba(0,0,0,0.4);
        }

        .movie-card img {
            height: 400px;
            width: 100%;
            object-fit: cover;
            border-bottom: 1px solid var(--border-color);
        }

        .rating-badge {
            position: absolute;
            top: 1rem;
            right: 1rem;
            background: rgba(0,0,0,0.7);
            backdrop-filter: blur(4px);
            padding: 0.2rem 0.6rem;
            border-radius: 6px;
            font-size: 0.8rem;
            font-weight: 700;
            color: #ffc107;
            border: 1px solid rgba(255,255,255,0.1);
        }

        .btn-action {
            border-radius: 6px;
            font-weight: 600;
            font-size: 0.8rem;
            padding: 0.5rem;
            flex: 1;
        }

        .btn-edit {
            background: #333;
            color: white;
            border: 1px solid #444;
        }

        .btn-edit:hover {
            background: #444;
            color: white;
        }

        .btn-delete {
            background: rgba(220, 53, 69, 0.1);
            color: #dc3545;
            border: 1px solid rgba(220, 53, 69, 0.2);
        }

        .btn-delete:hover {
            background: #dc3545;
            color: white;
        }

        footer {
            border-top: 1px solid var(--border-color);
            padding: 2rem 0;
            color: var(--text-dim);
            font-size: 0.85rem;
            text-align: center;
            margin-top: 4rem;
        }
    </style>
</head>

<body>

    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">CINESTREAM</a>
            
            <div class="search-container mx-auto d-none d-md-block">
                <input class="form-control search-input" placeholder="Search by title, genre or director...">
            </div>

            <div class="d-flex align-items-center gap-3">
                <span class="text-dim d-none d-sm-inline small">Welcome back</span>
                <div class="rounded-circle bg-danger" style="width: 32px; height: 32px;"></div>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        
        <div class="form-card shadow-lg">
            <h4 class="fw-bold mb-4">Post New Content</h4>
            <form action="/add" method="POST" enctype="multipart/form-data">
                <div class="row g-3">
                    <div class="col-md-6">
                        <label class="form-label small text-dim">Title</label>
                        <input class="form-control" name="title" placeholder="e.g. Inception" required>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label small text-dim">Genre</label>
                        <input class="form-control" name="genre" placeholder="e.g. Sci-Fi, Action">
                    </div>
                    <div class="col-md-4">
                        <label class="form-label small text-dim">Rating</label>
                        <input class="form-control" type="number" step="0.1" name="rating" placeholder="0.0 - 10.0">
                    </div>
                    <div class="col-md-4">
                        <label class="form-label small text-dim">Director</label>
                        <input class="form-control" name="director" placeholder="Director name">
                    </div>
                    <div class="col-md-4">
                        <label class="form-label small text-dim">Release Year</label>
                        <input class="form-control" type="number" name="releaseYear" placeholder="YYYY">
                    </div>
                    <div class="col-12">
                        <label class="form-label small text-dim">Description</label>
                        <textarea class="form-control" name="description" rows="2" placeholder="Brief synopsis..."></textarea>
                    </div>
                    <div class="col-12">
                        <label class="form-label small text-dim">Movie Poster</label>
                        <input class="form-control" type="file" name="poster">
                    </div>
                    <div class="col-12 mt-4">
                        <button class="btn btn-primary w-100">Publish Movie</button>
                    </div>
                </div>
            </form>
        </div>

        <h2 class="section-title">Trending Now</h2>
        <div class="row g-4">
            <% allMovies.forEach(movie => { %>
                <div class="col-xl-3 col-lg-4 col-md-6">
                    <div class="movie-card">
                        <div class="position-relative">
                            <img src="<%= movie.poster %>" alt="<%= movie.title %>">
                            <div class="rating-badge">
                                <i class="bi bi-star-fill me-1"></i><%= movie.rating %>
                            </div>
                        </div>
                        <div class="p-3">
                            <h6 class="fw-bold mb-1 text-truncate"><%= movie.title %></h6>
                            <div class="d-flex justify-content-between align-items-center mb-3">
                                <span class="text-dim small"><%= movie.genre %></span>
                                <span class="text-dim small"><%= movie.releaseYear %></span>
                            </div>
                            
                            <div class="d-flex gap-2">
                                <a href="/edit/<%= movie.id %>" class="btn btn-edit btn-action">
                                    <i class="bi bi-pencil me-1"></i>Edit
                                </a>
                                <a href="/delete/<%= movie.id %>" class="btn btn-delete btn-action">
                                    <i class="bi bi-trash me-1"></i>Delete
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

    <footer>
        <div class="container">
            <p class="mb-1">CINESTREAM DASHBOARD v1.0</p>
            <p class="small text-muted">&copy; 2025 All rights reserved. Designed for modern streaming experiences.</p>
        </div>
    </footer>

</body>
</html>